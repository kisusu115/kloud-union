const express = require('express');
//const { getStationInfo, getArrivalTimes } = require('../controllers/subwayController');

const router = express.Router();

//router.get('/info', getStationInfo);

//router.post('/times', getMethodName);

module.exports = router;
